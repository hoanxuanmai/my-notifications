# Backend .env example
# Copy to .env for local dev, or use these keys
# when configuring environment variables on free hosts
# (Render, Railway, Fly.io, etc.).

# ── Core server ─────────────────────────────────────
# HTTP port for NestJS backend
PORT=3000

# Node environment
NODE_ENV=production

# ── Database (PostgreSQL) ───────────────────────────
# Local example; on cloud, paste the DATABASE_URL from your Postgres service
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/my_notifications?schema=public"

# ── Auth (JWT) ──────────────────────────────────────
# Change this to a long random string in production
JWT_SECRET=change-me-in-production
# Token lifetime
JWT_EXPIRES_IN=7d

# ── CORS / Frontend origin ──────────────────────────
# Local: http://localhost:3001
# Production: your frontend URL, e.g. https://my-frontend.onrender.com
FRONTEND_URL=http://localhost:3001

# ── Redis (BullMQ queues) ───────────────────────────
# Local Redis
REDIS_HOST=localhost
REDIS_PORT=6379
# On cloud, set host/port from your Redis instance.

# ── Web Push (VAPID) ────────────────────────────────
# Generate locally inside backend/ with:
#   pnpm generate:vapid
WEB_PUSH_PUBLIC_KEY=your_public_vapid_key_here
WEB_PUSH_PRIVATE_KEY=your_private_vapid_key_here
# Optional contact email for web-push library
WEB_PUSH_CONTACT_EMAIL=mailto:admin@example.com
